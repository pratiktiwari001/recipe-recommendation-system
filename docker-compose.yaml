services:
  recipicks:
    image: pratiktiwari001/recipicks-app:latest
    container_name: recipicks
    ports:
      - "5173:5173"
    volumes:
      - new_volume:/app/src/components
    networks:
      - new-net

volumes:
  new_volume:

networks:
  new-net:
    driver: bridge
